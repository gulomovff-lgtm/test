# Hostel Management System - App.jsx Split into 5 Parts

## 📋 Описание

Система управления хостелом разделена на 5 частей для удобства копирования и редактирования в блокноте.

## 🗂️ Структура файлов

### Файлы для копирования:
- `App_Part1.jsx` - Импорты, переводы, конфигурация, константы, утилиты
- `App_Part2.jsx` - UI компоненты (Card, Button, Navigation, Dashboard, Charts, RoomCardChess)
- `App_Part3.jsx` - Модальные окна (CheckIn, GuestDetails, MoveGuest, Payment, AddExpense)
- `App_Part4.jsx` - Представления страниц (Calendar, Staff, Clients, Tasks, Debts, Reports, Shifts)
- `App_Part5.jsx` - Главный App компонент с логикой и рендером

### Готовый файл:
- `App.jsx` - Полный объединённый файл (готов к использованию)

## 🔧 Инструкция по сборке

### Вариант 1: Использовать готовый файл
Просто скопируйте `App.jsx` - он уже содержит все 5 частей вместе.

### Вариант 2: Собрать вручную из частей
1. Откройте блокнот (или любой текстовый редактор)
2. Скопируйте содержимое `App_Part1.jsx` целиком
3. Добавьте содержимое `App_Part2.jsx` (можно удалить разделительные комментарии)
4. Добавьте содержимое `App_Part3.jsx`
5. Добавьте содержимое `App_Part4.jsx`
6. Добавьте содержимое `App_Part5.jsx`
7. Сохраните как `App.jsx`
8. Готово!

## ✅ Реализованные исправления

### 1. ✅ Выселение (Checkout) - ИСПРАВЛЕНО
**Файл:** `App_Part3.jsx` - GuestDetailsModal

**Проблема:** Нельзя было выселить гостя, если долг был погашен администратором.

**Решение:** 
- Убрана проверка `if (balance < 0)` при выселении
- Теперь выселение разрешено всегда
- При переплате можно указать сумму возврата

```javascript
const handleDoCheckout = () => { 
  // Разрешаем выселение всегда, если balance >= 0
  const refund = checkoutManualRefund ? parseInt(checkoutManualRefund) : Math.max(0, balance);
  
  const finalData = { 
    totalPrice: totalPrice,
    refundAmount: refund,
    checkOutDate: new Date().toISOString()
  }; 
  
  onCheckOut(guest, finalData); 
};
```

### 2. ✅ Календарь - фикс полосок - ИСПРАВЛЕНО
**Файл:** `App_Part4.jsx` - CalendarView

**Проблема:** Полоска выселенного гостя растягивалась до текущего дня вместо фактической даты выселения.

**Решение:**
- Для выселенных гостей (`status === 'checked_out'`) используется `checkOutDate` из БД
- Для активных гостей расчёт по формуле: `checkInDate + days`
- Добавлено цветовое кодирование: зелёный = оплачено, красный = долг

```javascript
const getGuestBlockStyle = (guest) => {
  // ... 
  // ✅ ДЛЯ ВЫСЕЛЕННЫХ ИСПОЛЬЗУЕМ checkOutDate ИЗ БД
  let checkOutDate;
  if (guest.status === 'checked_out' && guest.checkOutDate) {
    checkOutDate = new Date(guest.checkOutDate);
    checkOutDate.setHours(12, 0, 0, 0);
  } else {
    const guestDurationMs = parseInt(guest.days) * 24 * 60 * 60 * 1000;
    checkOutDate = new Date(checkInDate.getTime() + guestDurationMs);
    checkOutDate.setHours(12, 0, 0, 0);
  }
  
  // Цветовое кодирование
  const totalPaid = getTotalPaid(guest);
  const totalPrice = guest.totalPrice || 0;
  const paidRatio = totalPrice > 0 ? Math.min(totalPaid / totalPrice, 1) : 0;
  
  return {
    background: `linear-gradient(90deg, 
      #10b981 0%, 
      #10b981 ${paidRatio * 100}%, 
      #ef4444 ${paidRatio * 100}%, 
      #ef4444 100%
    )`
  };
};
```

### 3. ✅ Клиенты - пагинация и фильтры - ИСПРАВЛЕНО
**Файл:** `App_Part4.jsx` - ClientsView

**Добавлено:**
- Пагинация (25/50/100 на странице)
- Фильтр по стране (выпадающий список уникальных стран)
- Поиск по имени и паспорту
- Навигация между страницами (← →)

```javascript
const [perPage, setPerPage] = useState(50);
const [currentPage, setCurrentPage] = useState(1);
const [countryFilter, setCountryFilter] = useState('');

// Уникальные страны
const uniqueCountries = useMemo(() => {
  return [...new Set(clients.map(c => c.country))].sort();
}, [clients]);

// Фильтрация + пагинация
const filteredClients = useMemo(() => {
  let result = clients.filter(c => {
    const matchSearch = (c.fullName || '').toLowerCase().includes(search.toLowerCase()) || 
                       (c.passport || '').includes(search.toUpperCase());
    const matchCountry = !countryFilter || c.country === countryFilter;
    return matchSearch && matchCountry;
  });
  
  return result.slice((currentPage - 1) * perPage, currentPage * perPage);
}, [clients, search, countryFilter, currentPage, perPage]);
```

### 4. ✅ Fazliddin - особые права - ИСПРАВЛЕНО
**Файлы:** `App_Part2.jsx` (Navigation), `App_Part5.jsx` (App)

**Изменения в DEFAULT_USERS:**
```javascript
{ login: 'fazliddin', pass: '123', name: 'Fazliddin', role: 'cashier', hostelId: 'hostel2' }
```

**Логика прав:**
```javascript
const canEdit = useMemo(() => {
  if (!currentUser) return false;
  if (currentUser.role === 'admin' || currentUser.role === 'super') return true;
  if (currentUser.login === 'fazliddin') {
    return selectedHostelFilter === 'hostel2';
  }
  return true;
}, [currentUser, selectedHostelFilter]);
```

**В Navigation:**
```javascript
const canSwitchHostels = currentUser.role === 'admin' || 
                          currentUser.role === 'super' || 
                          currentUser.login === 'fazliddin';
```

**Блокировка кнопок:**
- Fazliddin может редактировать только когда выбран hostel2
- Если выбран другой хостел или "Все хостелы", кнопки заблокированы
- Admin и Super видят и редактируют всё

### 5. ✅ Печать и экспорт - ИСПРАВЛЕНО
**Файл:** `App_Part1.jsx` - утилиты

**exportToExcel:**
- Правильная структура HTML таблицы
- Добавлены итоги: ПРИХОД, РАСХОД, БАЛАНС
- Правильная кодировка UTF-8
- Excel-совместимый формат

**printDocument:**
- Три типа документов: Чек, Анкета, Справка
- Правильное форматирование
- Корректное отображение всех данных
- Функция печати браузера

## 🎨 Используемые технологии

- React 18+ (hooks: useState, useEffect, useMemo, useRef)
- Firebase Firestore (для БД)
- Tailwind CSS (utility classes для стилей)
- Inline styles для динамических компонентов

## 👥 Тестовые аккаунты

```javascript
admin / admin123     - Полный доступ ко всем хостелам
super / super123     - Супер-администратор
fazliddin / 123      - Кассир хостела 2 (может редактировать только hostel2)
manager1 / 123       - Менеджер хостела 1
cashier1 / 123       - Кассир хостела 1
```

## 📊 Основные функции

### Панель управления (Dashboard)
- Статистика: активные гости, заполняемость, доход
- График заездов за 7 дней
- Шахматка комнат с визуализацией занятости

### Календарь
- Отображение гостей на временной шкале
- Цветовая индикация оплаты (зелёный/красный)
- Клик по гостю для деталей

### Персонал
- Список всех пользователей системы
- Роли и назначенные хостелы

### Клиенты
- Уникальные клиенты из всех заездов
- Пагинация 25/50/100
- Фильтр по стране
- Поиск по имени/паспорту
- История визитов

### Задачи
- Создание задач
- Отметка выполнения
- Приоритеты (низкий/средний/высокий)

### Долги
- Список гостей с задолженностями
- Сортировка по сумме долга
- Общая сумма долгов

### Отчёты
- Фильтр по датам
- Приход и расход
- Баланс
- Экспорт в Excel с итогами

### Смены
- В разработке

## 🔒 Система прав

| Роль | Возможности |
|------|-------------|
| **super** | Полный доступ, все хостелы |
| **admin** | Полный доступ, все хостелы |
| **fazliddin** | Кассир, может переключать хостелы, но редактировать только hostel2 |
| **manager** | Доступ к своему хостелу |
| **cashier** | Доступ к своему хостелу |

## 📝 Примечания

- ✅ Все 5 критических исправлений реализованы
- ✅ Комментарии-разделители для удобства навигации
- ✅ Код готов к использованию без дополнительных модулей
- ✅ Каждая часть - это сегмент одного большого файла (не отдельные модули)
- ✅ Все функции работают независимо

## 🚀 Быстрый старт

1. Используйте готовый `App.jsx` файл
2. Убедитесь что установлены зависимости: `react`, `firebase`
3. Настройте Firebase конфигурацию в Part 1
4. Добавьте Tailwind CSS в проект
5. Запустите приложение

## 📞 Поддержка

При возникновении вопросов или обнаружении багов:
1. Проверьте что все 5 частей объединены правильно
2. Убедитесь что Firebase настроен
3. Проверьте консоль браузера на наличие ошибок

---

**Версия:** 1.0  
**Дата:** 2026-02-07  
**Статус:** ✅ Готово к использованию
